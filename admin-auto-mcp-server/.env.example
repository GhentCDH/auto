# Admin Auto Instance Configuration

# Base URL of the Admin Auto instance (required)
AUTO_URL=https://auto.ghentcdh.be

# HTTP Basic Authentication credentials (required)
AUTO_USERNAME=your-username
AUTO_PASSWORD=your-password

# Transport mode: 'stdio' for local/CLI, 'http' for remote server (optional, default: stdio)
TRANSPORT=stdio

# Port for HTTP transport (optional, default: 3000)
PORT=3000
